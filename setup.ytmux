#!/bin/bash
tmux new-session \; split -h \; split -h \; split -h \; split -h \; select-layout main-vertical \; select-pane -t 1 \; split -v \; select-pane -t 1 \; send-keys "dev; cd ~/smarkets/services/events/event-manager; paver shell" \; send-keys Enter \; select-pane -t 2 \; send-keys "dev; cd ~/smarkets/services/events/api; paver shell" \; send-keys Enter \; select-pane -t 3 \; send-keys "dev; cd ~/smarkets/services/events/mapper; paver shell" \; send-keys Enter \; select-pane -t 4 \; send-keys "dev; cd ~/smarkets/services/cscs/neomin; paver shell" \; send-keys Enter \; select-pane -t 5 \; send-keys "dev; cd ~/smarkets/services/events/snowflakes; nix-shell" \; send-keys Enter \; select-pane -t 6 \; send-keys "dev; cd ~/smarkets/services/events/" \; send-keys Enter \; select-pane -t 1 \; send-keys "dev; ./app" \; send-keys Enter